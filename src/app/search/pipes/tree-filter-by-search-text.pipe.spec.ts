import { TreeFilterBySearchTextPipe } from './tree-filter-by-search-text.pipe';

describe('TreeFilterBySearchTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TreeFilterBySearchTextPipe();
    expect(pipe).toBeTruthy();
  });
});
